# DAY - 05 QUESTIONS

## **Easy Level (1–8)**

### **1. Which module is commonly used to create GUI applications in Python?**

1. pygame
2. tkinter
3. flask
4. numpy

**answer: `2. tkinter`**

### **2. Which function is used to create a Button in Tkinter?**

1. `tk.Button()`
2. `Button.create()`
3. `new Button()`
4. `makeButton()`

**answer: `1. tk.Button()`**

### **3. Which library is used for sending HTTP requests while scraping?**

1. smtplib
2. tkinter
3. requests
4. shutil

**answer: `3. requests`**

### **4. What is BeautifulSoup mainly used for?**

1. File copying
2. Parsing HTML
3. Sending emails
4. GUI creation

**answer: `2. Parsing HTML`**

### **5. Which module helps in automating file operations like copy and move?**

1. os
2. tkinter
3. shutil
4. smtplib

**answer: `3. shutil`**

### **6. Which function sends an email using SMTP?**

1. `sendmail()`
2. `mailSend()`
3. `send()`
4. `email()`

**answer: `1. sendmail()`**

### **7. In Tkinter, which method starts the GUI event loop?**

1. `run()`
2. `start()`
3. `loop()`
4. `mainloop()`

**answer: `4. mainloop()`**

### **8. Which function is used to list files inside a folder?**

1. `os.path()`
2. `os.listdir()`
3. `shutil.show()`
4. `os.get()`

**answer: `2. os.listdir()`**

---

### **9. What does `BeautifulSoup(response.text, "html.parser")` do ?**

1. Downloads files
2. Converts HTML to Python objects
3. Deletes HTML tags
4. Encrypts HTML

**answer: `2. Converts HTML to Python objects`**

### **10. In Tkinter, which layout method places widgets inside a grid ?**

1. `pack()`
2. `grid()`
3. `place()`
4. `row()`

**answer: `2. grid()`**

### **11. Which module helps in automating directory creation, removal, etc. ?**

1. shutil
2. os
3. smtplib
4. re

**answer: `2. os`**

### **12. What does `smtplib.SMTP("smtp.gmail.com", 587)` represent?**

1. A GUI window
2. A scraping request
3. A mail server connection
4. A file copy operation

**answer: `3. A mail server connection`**

### **13. Which function in BeautifulSoup retrieves all `<a>` tags?**

1. `find()`
2. `get()`
3. `find_all("a")`
4. `find_links()`

**answer: `3. find_all("a")`**

### **14. Which Tkinter widget is best for multiline text input?**

1. `Entry`
2. `Label`
3. `Text`
4. `Box`

**answer: `3. Text`**

### **15. What does `shutil.copy("a.txt", "backup/")` do?**

1. Compress the file
2. Copy a.txt to backup folder
3. Delete a.txt
4. Move a.txt

**answer: `2. Copy a.txt to backup folder`**

### **16. In a Student Management System, which data structure is best to store student details?**

1. list
2. tuple
3. dictionary
4. set

**answer: `3. dictionary`**

---

### **17. What is the purpose of `os.walk()` in automation scripts?**

1. Automate email sending
2. Recursively iterate through directories
3. Extract zip files
4. Format console output

**answer: `2. Recursively iterate through directories`**

### **18. In Tkinter, which method is used to update Label text dynamically?**

1. `label.change()`
2. `label.update_text()`
3. `label.config(text="new")`
4. `label.reload()`

**answer: `3. label.config(text="new")`**

### **19. Which BeautifulSoup method is best for extracting nested HTML tags?**

1. `select()`
2. `pull()`
3. `html()`
4. `nested()`

**answer: `1. select()`**

### **20. What is required to send mail using Gmail SMTP in Python?**

1. Port number only
2. App password + TLS
3. Only username
4. HTML parser

**answer: `2. App password + TLS`**

### **21. Which automation task is correctly done with `shutil.rmtree()`?**

1. Rename file
2. Delete entire folder with contents
3. Scan folder
4. Email folder

**answer: `2. Delete entire folder with contents`**

### **22. In Tkinter MVC design, the "Controller" mainly handles:**

1. GUI layout
2. Database storage
3. Business logic & events
4. Styling

**answer: `3. Business logic & events`**

### **23. In scraping, what is the biggest risk of using `requests` without headers?**

1. Large file size
2. Getting blocked by website
3. Low memory usage
4. Slow Tkinter UI

**answer: `2. Getting blocked by website`**

### **24. In final project integration, which principle ensures code reusability?**

1. Hardcoding
2. Encapsulation
3. Copy-paste coding
4. Repetition

**answer: `2. Encapsulation`**

### **25. A Billing App needs persistent storage. Which is the most appropriate?**

(For beginners)

1. Lists
2. CSV files
3. Sets
4. Tuples

**answer: `2. CSV files`**
