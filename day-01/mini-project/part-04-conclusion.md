# 🪄 **Next Level Ideas (Optional Enhancements)**

You can later add:

* 🧾 use pickle to store the data   (save result).
* 📊 Percentage calculation         (based on max marks per subject).
* 🧮 Input validation               (reject negative or >100 marks).
* 🎓 Name input                     (show student name in the report).
* reset db option